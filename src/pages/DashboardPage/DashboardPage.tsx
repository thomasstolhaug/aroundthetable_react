import React from "react";
import { Typography } from "antd";

const DashboardPage: React.FC = () => {
	return <Typography.Title level={1}>Dashboard</Typography.Title>;
};

export default DashboardPage;
